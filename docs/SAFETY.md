# Public safety boundary

Examples and probes use synthetic repository labels, invented SHAs, synthetic paths,
and secret fingerprints only. The project contains no account, credential, private
repository, network endpoint, remote mutation, or production data. The CLI is fully
offline and mutates only paths explicitly supplied to local `apply` or `rollback`.

